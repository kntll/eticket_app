<h1>Listing tickets</h1>

<table>
  <thead>
    <tr>
      <th>Pdf</th>
      <th>Price</th>
      <th>User</th>
      <th>Event</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tickets.each do |ticket| %>
      <tr>
        <td><%= ticket.pdf %></td>
        <td><%= ticket.price %></td>
        <td><%= ticket.user_id %></td>
        <td><%= ticket.event_id %></td>
        <td><%= link_to 'Show', ticket %></td>
        <td><%= link_to 'Edit', edit_ticket_path(ticket) %></td>
        <td><%= link_to 'Destroy', ticket, method: :delete, data: { confirm: 'Weet je dit zeker?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ticket', new_ticket_path %>
